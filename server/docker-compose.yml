services:
  relay:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - relay-data:/data
    environment:
      - PORT=8080
      - DB_PATH=/data/relay.db
    restart: unless-stopped

volumes:
  relay-data:
